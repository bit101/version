#! /bin/bash 

case $1 in
java)
  $1 -version
  ;;

gcc | rustc | dotnet)
  $1 --version
  ;;

node | perl | lua)
  $1 -v
  ;;

python)
  $1 -V
  ;;

go)
  $1 version
  ;;

*)
  echo "'$1' does not seem to be installed."
  ;;

esac
